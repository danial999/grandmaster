#Readme
Chrome open without security errors
chrome.exe --user-data-dir="C:/Chrome dev session" --disable-web-security

Local Server run to enable reqding the file with http:
cd to Directory 
Run :
python -mSimpleHTTPServer

